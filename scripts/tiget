#!/usr/bin/env python
import sys

from tiget.repl import Interpreter, Repl


def main():
    args = sys.argv[1:]
    if not args or args[0] == 'shell':
        Repl().run_loop()
    else:
        Interpreter().run(args)


if __name__ == '__main__':
    main()
